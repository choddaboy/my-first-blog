"""Views: plave to put the "logic" of an application
 -requests information from the 'model' and pass it to a 'template'. """


#from django.shortcuts import render


# Create your views here.

#def port_list(request):
 #   return render(request, 'blog1/post_list.html', {})

    #"""created a function ( def ) called 'post_list' that takes
    #'request' and 'return' a function ('render') that will
    #render (put together) our template (blog1/post_list.html)."""
